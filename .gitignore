.DS_Store
__pycache__/
*.pyc
.venv/
venv/

# Raw UDP capture from rec.py — machine-specific and large.
wsjtx_log.txt
mock_raw_listener_output.txt
.pytest_cache/

# Voided contacts are a judgement made during one event, not source.
voided.json

# Runtime state: the admin password, the fixture list, voided contacts.
# Belongs on a volume, never in the repository.
data/
